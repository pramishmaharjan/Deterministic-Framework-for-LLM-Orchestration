# Required for the Surgical Brain OS Full Distribution
polars>=1.0.0
sqlglot>=20.0.0
sqlfluff>=3.0.0
python-dotenv
pathlib
# Orchestration & Graphing
langgraph
langchain-core
# Note: langstruct is typically handled via specialized internal SDKs
# or specific GitHub installs. We include the base requirements here.
